import * as types from "./actionTypes";

const initialState = {};

function reduce(state = initialState, action = {}) {
  switch (action.type) {
    default:
      return state;
  }
}

export default reduce;
